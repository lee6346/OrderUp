const ROOT_URL = 'http://localhost:5000';

export const REGISTER_URL = `${ROOT_URL}/auth/register`;
export const LOGIN_URL = `${ROOT_URL}/auth/login`;

export const UPLOAD_IMAGE = `${ROOT_URL}/api/upload`;
